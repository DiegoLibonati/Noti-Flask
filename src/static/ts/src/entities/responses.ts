export type DeleteNoteResponse = {
  code: string;
  message: string;
  redirect_to: string;
};

export type PatchUpdateNoteResponse = {
  code: string;
  message: string;
  redirect_to: string;
};

export type PostCreateNoteResponse = Response;
